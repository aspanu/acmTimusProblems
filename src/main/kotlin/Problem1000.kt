import java.util.*

class Problem1000 {
    fun run(input: String): Int {
        val scan = Scanner(input)
        val opA = scan.nextInt()
        val opB = scan.nextInt()
        return opA * opB
    }
}